{
  "name": "@locol/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for LOCOL Workspace — gives Claude Desktop/Code access to CRUD contacts, opportunities, organizations, notes, milestones, and groups.",
  "type": "module",
  "bin": {
    "locol-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
